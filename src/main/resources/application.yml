spring:

  datasource:
    url: jdbc:postgresql://localhost:5432/smartjobtracker
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: validate

    show-sql: true

  flyway:
    enabled: true
    baseline-on-migrate: true

  mail:
    host: smtp.example.com
    port: 587
    username: demo
    password: demo

server:
  port: 8080